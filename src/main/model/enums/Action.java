package model.enums;

// Enum class representing possible actions that can be associated with a single move
// There are plans to expand this to castling and more in the future
public enum Action {
    CHECK,
    CHECKMATE,
    TAKE
}
